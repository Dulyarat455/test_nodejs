BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[SaleTemp] ADD [addedMoney] INT,
[tasteId] INT;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
